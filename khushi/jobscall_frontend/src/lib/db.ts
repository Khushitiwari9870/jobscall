// Database operations will be handled through Django REST API
// This is a placeholder file to maintain compatibility with existing imports

export const db = {
  // Add any client-side database utilities here if needed
  // All database operations should go through the API client
};
