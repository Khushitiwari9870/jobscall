from django.apps import AppConfig


class ImmediateAvailableConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.immediate_available'
    verbose_name = 'Immediate Available Candidates'
